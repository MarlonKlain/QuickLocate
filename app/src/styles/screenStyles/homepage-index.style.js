import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  welcomeContainer: {
    flex: 1,
    alignItems: "center",
    justifyContent: "center",
  },
  quickLocateImg: {
    resizeMode: "center",
    width: 352,
    height: 400,
    marginTop: 50,
    marginBottom: 50,
  },
});
